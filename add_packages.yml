---
- hosts: dev
  user: harm
  sudo: yes
  tasks:
    - apt: pkg=$item state=installed
      with_items:
        - apt-file
        - htop
        - dnsutils
        - curl
        - ack-grep
        - tmux
